/*
import React, { useContext } from 'react';


const Form = () => {
    const [badget, setBadget] = useState('');
    const user = useContext(AuthContext);
   
    const handleSubmit = (e) => {
e.preventDefault();
if (TextEncoder.length === 0) {
    alert('plese fill in blank');
    return;
}
    }
    return (
        <form onSubmit={handleSubmit}>
            <textarea></textarea>
            <button>送信</button>
        </form>
    );
};
export default Form;



*/


